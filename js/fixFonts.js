function fixFontSize() {
    if (navigator.appVersion.indexOf("Mac")==-1) { document.getElementById("josephCrane").setAttribute("font-size", "20rem"); }
    /*
        This kind of feels like a dirty hack and realistically I should just have picked
        a different font, however Julieta Ulanovsky did a great job with Montserrat so
        here we are. 
    */
}